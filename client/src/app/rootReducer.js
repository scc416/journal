import mode from "features/mode/modeSlice";
import auth from "features/auth/authSlice";
import error from "features/error/errorSlice";

const reducers = { mode, auth, error };

export default reducers;
